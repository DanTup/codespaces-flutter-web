{
	"name": "Flutter",
	"settings": {
		"dart.flutterSdkPath": "~/flutter",
		"terminal.integrated.env.linux": {
			"PATH": "~/flutter/bin/:${env:PATH}"
		}
	},
	"postCreateCommand": "git clone --branch master https://github.com/flutter/flutter ~/flutter && flutter config --enable-web && flutter packages get",
	"extensions": [
		"Dart-Code.dart-code",
		"Dart-Code.flutter"
	]
}
